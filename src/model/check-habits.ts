/**
 * Returns true if you did [projectName] for at least [minTime]
 *  minutes on [day], false otherwise.
 */
export function checkHabitMinTime(
  day: Date,
  projectName: string,
  minTime: number
) {
  return true
}
